# OPTIMADE MCP Client 依赖包
# ================================

# MCP协议支持
mcp[cli]

# OpenAI SDK
openai

# 环境变量管理
python-dotenv

# 异步支持（Python 3.7+内置，但为了兼容性列出）
# asyncio (内置)
# typing (内置)
# pathlib (内置)
# logging (内置)
# json (内置)
# os (内置)
# sys (内置)
# contextlib (内置)
